Альтшуль Даниил Евгеньевич\
j3117\
ИСПОЛЬЗОВАННЫЕ КОМАДНЫ ДЛЯ РАБОТЫ 

добавить все файлы 
<pre> git add .</pre> 
  создать коммит с какой то надписью
<pre> git commit -m  </pre> 
  посмотреть ветки 
<pre>  git branch </pre> 

  меняет ветку на другую
<pre>  git switch "имя ветки "  </pre> 
  создает ветку с именем
<pre>  git branch "имя ветки "  </pre> 
  посмотреть что изменилось и что добавилось в коммит
<pre>  git status  </pre> 
  складывает 2 ветки в одну 
<pre>  git merge "имя ветки" </pre> 
  переносит коммиты с одной ветки на другую
<pre>  git rebase "имя ветки" </pre> 

